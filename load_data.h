#include <main.h>

std::vector<std::vector<std::vector<Complex>>>load_data(int dim1, int dim2, int dim3, std::string path);
//
//template<typename T>
//void slice(std::vector<T>& dest, const std::vector<T>& src, size_t rows, size_t cols, size_t start_row, size_t start_col, size_t end_row, size_t end_col);
